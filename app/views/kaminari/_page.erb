<li class="page<%= 'current' if page.current? %>">
  <% if page.current? %>
    <%= link_to page, "#" %>
  <% else %>
    <%= link_to_unless page.current?, page, url, {:remote => remote, :rel => page.next? ? '다음' : page.prev? ? '앞' : nil} %>
  <% end %>
</li>